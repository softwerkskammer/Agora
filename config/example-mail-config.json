// to use these values, strip "example-" from the filename, delete this comment and use / adapt one of the two alternatives below

// first alternative: SMTP - configuration: https://github.com/andris9/Nodemailer#setting-up-smtp
{
  "transport": "SMTP",
  "transport-options": {
    "service": "iCloud",
    "auth": {
      "user": "",
      "pass": ""
    },
    "debug": "true"
  }
}

// second alternative: sendmail - configuration: https://github.com/andris9/Nodemailer#setting-up-sendmail
{
  "transport": 'sendmail',
  "transport-options": {}
}
