#login.modal.fade(tabindex="-1")
  .modal-dialog
    .modal-content
      .modal-header
        button.close(type="button", data-dismiss="modal", aria-hidden="true") &times;
        h3 #{t("authentication.authentication")}
      .modal-body
        -var returnUrl = (typeof(currentUrl) === 'undefined') ? "/" : currentUrl
        .row
          .col-md-12
            p #{t("authentication.authentication_text")}
        .row
          .col-md-3
            a.btn.btn-default(href="/auth/openid?openid_identifier=" + encodeURIComponent("https://www.google.com/accounts/o8/id") + "&returnTo=" + encodeURIComponent(returnUrl))
              img(src="/img/google.ico", alt="google")
              | &nbsp;Google
          .col-md-3
            a.btn.btn-default(href="/auth/github?returnTo=" + encodeURIComponent(returnUrl))
              img(src="/img/github.ico", alt="github")
              | &nbsp;Github
        .row
          .col-md-12
            form(action="/auth/openid", method="get")
              p &nbsp; 
              input(type="hidden", name="returnTo", value=returnUrl)
              .panel.panel-default
                .panel-heading
                  a.accordion-toggle(href="#openIDinput", data-toggle="collapse") #{t("authentication.alternative_url")}:
                #openIDinput.collapse.panel-collapse
                  .input-group
                    input.form-control(id="openid_identifier", type='text', name="openid_identifier")
                    .input-group-btn
                      button.btn.btn-default(type="submit") #{t("authentication.authenticate")}
