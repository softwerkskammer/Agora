doctype html
html
  body
    h3 SoCraTes Waitinglist Confirmation
    p.
      Hello #{member.displayName()},
    p Congratulations! You entered the waitinglist for the SoCraTes Conference 2017.
    p
      if bookingdetails.desiredRooms.length === 1
        | You are waiting for a #{' '}
        b #{bookingdetails.desiredRooms[0].room}
        | .
      else
        | You are waiting for #{' '}
        b #{bookingdetails.desiredRooms.map(room => room.room).join(', ')}
      | #{' '} for #{' '}
      b #{bookingdetails.desiredRooms[0].nights}
      | #{' '} nights until #{' '}
      b #{bookingdetails.desiredRooms[0].until}

    p Once we have drawn the lottery and you get a spot, we will book you a room.
    p You can review and modify your waitinglist data at #{' '}
      a(href=url + '/members/edit') #{url}/members/edit

    p The organisers
