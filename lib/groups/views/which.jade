extend ../../../views/layout

block title
  | #{title}

block content
  h1 #{title}

  ul
    each list in lists
      li= list.homepage
      li= list.listAddress
      li= list.subject
      li= list.isSubscriber
      li= list.isOwner
      li= list.isEditor
      li= list.bounceCount