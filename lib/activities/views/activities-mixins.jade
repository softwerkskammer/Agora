mixin visitorCount(anzahl)
  p Bislang
    if (anzahl === 0)
      |  gibt es keine Teinahmezusagen.
    else if (anzahl === 1)
      |  hat ein Mitglied seine Teilnahme zugesagt.
    else
      |  haben #{anzahl} Mitglieder ihre Teilnahme zugesagt.

mixin subscriptionButtons(registeredMembers, url, visitors, currentMemberId)
  if (visitors.length > 0 && registeredMembers.indexOf(currentMemberId) > -1)
    a.btn.btn-primary(href="unsubscribe/#{encodeURIComponent(url)}") Ich kann doch nicht…
  else
    a.btn.btn-primary(href="subscribe/#{encodeURIComponent(url)}") Ich bin dabei!
