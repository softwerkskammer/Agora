extends ../../../views/layout

include ./mailheader-mixin


block title
  | #{group} #{t("mailarchive.emails")}

block content
  .row
    .col-md-12
      .page-header
        mixin pageHeader(group, t("mailarchive.unthreaded"))
      .panel.pre-scrollable
        mixin mailIndex(mailHeaders)
