extends ../../../views/layout

block title
  | Seite existiert nicht.

block content
  .row
    .col-md-12
      .page-header
        h2 Seite existiert nicht. &nbsp;
          small "#{pageName}" in Wiki "#{subdir}"
  .row
    .col-md-6
      h3 Nicht existente Wiki-Seite
      p Du wolltest eine Seite ansehen, die leider nicht existiert.
  .row
    .col-md-12
      a.btn.btn-default.pull-right(href="javascript:history.back()", type="cancel") Zurück

