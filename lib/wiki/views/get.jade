extends ../../../views/layout

block title
  | #{pageName} : #{subdir}

block content
  .row
    .col-md-12
      .page-header
        .btn-group.pull-right
            a.btn.btn-default(href="/wiki/list/#{subdir}/", title="Alle Seiten in \"#{subdir}\""): i.fa.fa-list.fa-fw
            a.btn.btn-default(href="/wiki/versions/#{subdir}/#{pageName}", title="Revisionen"): i.fa.fa-sort-amount-desc.fa-fw
            if (canEdit)
              a.btn.btn-default(href="/wiki/edit/#{subdir}/#{pageName}", title="Bearbeiten"): i.fa.fa-edit.fa-fw
        form.hidden-print.form-search.pull-right(action='/wiki/search', method='post')
          input.form-control(type="text", name="searchtext", placeholder="Wiki durchsuchen")
        h2 "#{pageName}"&nbsp;
          small in Wiki "#{subdir}"

  !{content}
